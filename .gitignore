*.iml
.gradle/
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
app/build/
app/src/enhanced/jniLibs/
# Never commit secrets
*.jks
*.keystore
secrets.properties
# Exception: the deliberately PUBLIC CI signing key (stable signature for
# sideloaded release builds — see app/build.gradle, NOT a secret)
!app/ci-signing.keystore
