<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"
        xmlns:tools="http://schemas.android.com/tools" />

    <application
        android:name=".QBApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- magnet links -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.addtorrent.AddTorrentActivity"
            android:exported="true"
            android:parentActivityName=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="magnet" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/x-bittorrent" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-bittorrent" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.detail.DetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.settings.ServerSettingsActivity"
            android:exported="false"
            android:label="@string/settings_server_connection"
            android:parentActivityName=".ui.main.MainActivity" />

        <!--
            Form-heavy preferences editor: surviving rotations without
            recreating the views keeps the user's in-progress edits intact.
        -->
        <activity
            android:name=".ui.qbsettings.QBSettingsActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:label="@string/qbt_settings_title"
            android:parentActivityName=".ui.main.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.stats.StatisticsActivity"
            android:exported="false"
            android:label="@string/stats"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.log.LogActivity"
            android:exported="false"
            android:label="@string/log_title"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.rss.RssActivity"
            android:exported="false"
            android:label="@string/rss_title"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.rss.RssArticlesActivity"
            android:exported="false"
            android:label="@string/rss_title"
            android:parentActivityName=".ui.rss.RssActivity" />

        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:label="@string/search_engine_title"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.search.SearchPluginsActivity"
            android:exported="false"
            android:label="@string/search_plugins"
            android:parentActivityName=".ui.search.SearchActivity" />

        <service
            android:name=".qbt.LocalEngineService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".qbt.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
