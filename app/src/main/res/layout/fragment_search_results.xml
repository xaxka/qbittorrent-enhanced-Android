<?xml version="1.0" encoding="utf-8"?>
<!-- Search results, qBC SearchResultScreen parity: toolbar with filter /
     sort / stop, a name-filter input that swaps in for the title, the
     always-visible "showing X of Y" count bar with the indeterminate
     progress line riding its top edge while the search runs,
     pull-to-refresh results, and a selection bottom bar (download
     selected / select all / inverse). No empty-list placeholder and no
     in-page state text — qBC shows none. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        app:liftOnScroll="false"
        app:statusBarForeground="?attr/colorSurface">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/app_bar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_arrow_back_24px">

            <!-- qBC SearchBar parity: when the search action is tapped the
                 borderless 18sp query field takes over the toolbar's title
                 slot INLINE (between the back arrow and the actions) — it
                 never drops below the app bar. -->
            <EditText
                android:id="@+id/search_input"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@null"
                android:gravity="center_vertical"
                android:hint="@string/search_filter_results"
                android:imeOptions="actionDone"
                android:importantForAutofill="no"
                android:inputType="text"
                android:maxLines="1"
                android:textSize="18sp"
                android:visibility="gone" />
        </com.google.android.material.appbar.MaterialToolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- qBC parity: the "showing X of Y" count bar is ALWAYS visible (it
         reads "0 of 0" until the first poll lands) and the thin
         indeterminate progress line rides its top edge while the search
         is running. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/result_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurface"
            android:paddingHorizontal="16dp"
            android:paddingVertical="4dp"
            android:textAppearance="?attr/textAppearanceBodySmall"
            tools:text="Showing 12 of 30" />

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/search_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            android:visibility="gone"
            tools:visibility="visible" />
    </FrameLayout>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- qBC parity: no empty-list placeholder on the results screen —
             the list is simply blank until (and unless) results arrive. -->
        <io.github.xixka.qbittorrent.ui.customviews.EmptyRecyclerView
            android:id="@+id/result_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingHorizontal="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Selection mode bottom bar, qBC parity: close, "N selected",
         download selected, select all + select inverse in overflow. -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/selection_bar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurfaceContainerHigh"
        android:visibility="gone"
        app:navigationIcon="@drawable/ic_close_24px"
        app:titleTextColor="?attr/colorOnSurface" />
</LinearLayout>
