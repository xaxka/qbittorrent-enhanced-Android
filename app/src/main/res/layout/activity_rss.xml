<?xml version="1.0" encoding="utf-8"?>
<!-- RSS hub, qBC RssFeedsScreen parity: Feeds tree + Rules tabs; the
     toolbar carries the qBC action set (add / refresh all / rules), and
     the bottom bar slides in during qBC's move mode ("select destination
     folder"). No FAB — adding happens from the toolbar like upstream. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        app:liftOnScroll="false"
        app:statusBarForeground="?attr/colorSurface">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/app_bar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_arrow_back_24px"
            app:title="@string/rss_title" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabContentStart="0dp"
            app:tabMode="fixed">

            <com.google.android.material.tabs.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/rss_tab_feeds" />

            <com.google.android.material.tabs.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/rss_tab_rules" />
        </com.google.android.material.tabs.TabLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- qBC move mode: tap a folder in the tree to move the picked item
         there; the back arrow (and the system back) cancel the move. -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/move_bar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_gravity="bottom"
        android:background="?attr/colorSurfaceContainerHigh"
        android:elevation="8dp"
        android:visibility="gone"
        app:navigationIcon="@drawable/ic_arrow_back_24px"
        app:title="@string/rss_select_folder"
        app:titleTextColor="?attr/colorOnSurface" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
