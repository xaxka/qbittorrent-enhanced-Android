<?xml version="1.0" encoding="utf-8"?>
<!-- RSS tab root, qBC RssFeedsScreen parity — feeds ONLY, no tabs and no
     pager: the toolbar carries the qBC action set (add / refresh all /
     rules), the list renders the subscription tree with the synthetic
     root "/" as its first card, and the bottom bar slides in during
     qBC's move mode ("select destination folder"). Rules is a separate
     pushed screen reached from the toolbar, like upstream. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        app:liftOnScroll="false"
        app:statusBarForeground="?attr/colorSurface">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/app_bar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="@string/rss_title"
            app:titleTextAppearance="?attr/textAppearanceTitleLarge"
            app:titleTextColor="?attr/colorOnSurface" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <io.github.xixka.qbittorrent.ui.customviews.EmptyRecyclerView
            android:id="@+id/feed_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- qBC move mode: tap a folder in the tree to move the picked item
         there; the back arrow (and the system back) cancel the move. -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/move_bar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_gravity="bottom"
        android:background="?attr/colorSurfaceContainerHigh"
        android:elevation="8dp"
        android:visibility="gone"
        app:navigationIcon="@drawable/ic_arrow_back_24px"
        app:title="@string/rss_select_folder"
        app:titleTextColor="?attr/colorOnSurface" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
